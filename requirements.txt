# Core
anthropic>=0.28.0
python-dotenv>=1.0.1
pydantic>=2.9.0
pydantic-settings>=2.5.0
numpy>=2.0.0
tqdm>=4.66.4
httpx>=0.27.0
aiofiles>=23.2.1

# Evaluation
pytest>=8.2.2
rank_bm25>=0.2.2

# Vector store
chromadb>=1.0.0

# Embeddings — priority chain: fastembed → sentence-transformers → BM25 fallback
# fastembed: ONNX-based, no torch, works on Python 3.13 (required on 3.13)
# sentence-transformers: torch-based, works on Python ≤3.12 with torch wheels
fastembed>=0.7.0
# sentence-transformers>=3.0.0   # uncomment on Python ≤3.12 with torch available

# LlamaIndex — wraps ChromaDB with a richer query / metadata-filter API
# llama-index-embeddings-huggingface is optional (sentence-transformers mode only)
llama-index-core>=0.10.0
llama-index-vector-stores-chroma>=0.1.0
# llama-index-embeddings-huggingface>=0.2.0  # optional: only needed with sentence-transformers

# UI — replaces React + FastAPI; run with: streamlit run streamlit_app.py
streamlit>=1.35.0
